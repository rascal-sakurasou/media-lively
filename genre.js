document.addEventListener("DOMContentLoaded",async()=>{const db=mediaLively.supabase,esc=mediaLively.escapeHTML,box=document.getElementById("genres");const {data}=await db.from("anime").select("id,title,english_title,genre,description");const set=new Set();(data||[]).forEach(a=>mediaLively.genres(a.genre).forEach(g=>set.add(g)));const genres=[...set].sort((a,b)=>a.localeCompare(b,"ja"));genres.forEach(g=>{const a=document.createElement("a");a.className="genre-pill";a.href=`genre.html?genre=${encodeURIComponent(g)}`;a.textContent=g;box.appendChild(a)});const q=new URLSearchParams(location.search).get("genre");if(!q)return;document.getElementById("genre-results").classList.remove("hidden");document.getElementById("genre-title").textContent=`「${q}」のアニメ`;const results=(data||[]).filter(a=>mediaLively.genres(a.genre).some(g=>mediaLively.normalize(g)===mediaLively.normalize(q)));const list=document.getElementById("genre-list");if(!results.length){list.innerHTML=`<div class="status">該当する作品がありません。</div>`;return}results.forEach(a=>{const el=document.createElement("a");el.className="list-card";el.href=mediaLively.detailURL(a.id);el.innerHTML=`<h2>${esc(a.title)}</h2>${a.english_title?`<div class="english-title">${esc(a.english_title)}</div>`:""}<p class="description">${esc(a.description||"")}</p><div class="detail">詳細を見る →</div>`;list.appendChild(el)})});